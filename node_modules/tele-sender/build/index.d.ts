export declare enum Telegram_ParseModes {
    MarkdownV2 = 0,
    HTML = 1
}
export default class Telegram {
    private readonly api;
    private updates?;
    constructor(api: string);
    send(id: number | string, text: string, parseMode?: Telegram_ParseModes): Promise<boolean>;
    findID(): Promise<Record<string, number>>;
    private request;
}
